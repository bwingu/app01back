# Application dependencies

require:
    - play
    - customModules -> Authenticate
    - exploit -> exploit
    #- play -> gae 1.4
    - play -> cloudbees 0.2.2
    - play -> db 1.1.1
    - play -> cobertura 2.4

repositories:

    - playCustomModules:
        type: local
        artifact: "/Users/sebastienlouis/DEV-APP/play-1.2.4/authenticate"
        contains:
            - customModules -> *
            
    - playExploit:
        type: local
        artifact: "/Users/sebastienlouis/DEV-APP/play-1.2.4/exploit"
        contains:
            - exploit -> *